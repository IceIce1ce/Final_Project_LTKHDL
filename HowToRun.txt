Activate the min_ds-env environment before launching to Jupyter Notebook. Install the below libraries before runing the House_Prices.ipynb

pip install mlxtend==0.19.0
pip install lightgbm==3.3.1
pip install xgboost==1.5.1